import React from "react";
import { Box } from "@mui/material";

/**
 * Sidebar component (deprecated)
 * Content has been moved to App.jsx
 * @deprecated This component is no longer used
 */
function Sidebar() {
  return (
    <Box>
      {/* Sidebar is no longer used - content moved to App.jsx */}
    </Box>
  );
}

export default Sidebar;
